# CLAUDE.md

## 项目背景

video-api-debugger 是一个面向视频生成 API 调试与运营管理的 Next.js 项目。当前代码结构包含视频任务创建与查询、资产上传与外链创建、工作区资产管理、用户登录态、后台用户管理、点数查询与调整、Provider 适配、Prisma 数据访问等模块。

本项目采用 Hermes -> Claude Code -> Codex 的协作方式：

- Hermes：定义任务目标、边界、允许修改范围、禁止修改范围与验收标准。
- Claude Code：默认做只读分析、方案审查、风险识别与结果复核。
- Codex：在 Hermes 明确授权后执行具体文件修改与验证。

## 技术栈判断

- 项目名称：video-api-debugger
- 前端与服务端框架：Next.js 14，使用 App Router 与 API Routes
- UI 层：React 18
- 语言：TypeScript
- 数据访问：Prisma 5
- 数据库：SQLite
- 包管理与脚本来源：`package.json`
- 已发现通用脚本：`lint`、`build`
- 未发现独立通用脚本：`typecheck`、`test`

## Claude Code 默认职责

- 默认以只读方式理解项目结构、业务流程、依赖关系与风险区域。
- 在 Codex 执行前，帮助 Hermes 判断任务是否清晰、是否越界、是否需要拆分、是否需要补充验收标准。
- 审查 Codex 的 diff 是否严格落在允许范围内，是否触及高风险业务逻辑，是否引入回滚风险。
- 对鉴权、点数、Provider、上传、数据库等区域进行重点风险提示。
- 对测试缺口给出具体建议，但不把建议扩展成未授权代码修改。
- 在无法确定事实时，明确说明“不确定”或“需要进一步只读确认”。

## 默认不要让 Claude Code 直接写代码

- Claude Code 默认不直接修改代码或文档，除非 Hermes 或用户明确授权。
- Claude Code 的主要价值是只读分析、边界检查、风险审查和任务拆解。
- 需要落地修改时，应由 Hermes 明确允许修改文件/目录，再交由 Codex 执行。
- 如果任务涉及高风险区域，应先让 Claude Code 给出只读分析与审查清单，再决定是否允许 Codex 修改。

## 当前项目高风险区域

- 鉴权 / `middleware`：涉及登录态、会话判断、接口访问边界与管理后台入口，错误改动可能导致未授权访问或正常用户无法访问。
- 点数 / credit 流程：涉及 `src/app/api/me/credits/**`、`src/app/api/admin/credits/**`、价格计算与扣减/调整记录，错误改动可能导致点数错扣、漏扣或越权调整。
- 上传与外链拉取：涉及资产上传、从 URL 创建资产、Provider 删除、工作区资产管理，错误改动可能导致任意文件、外链安全、资源泄露或资产归属错误。
- Provider 适配层：涉及 `src/lib/provider/**`、视频创建、任务状态查询、下载与重试逻辑，错误改动可能影响外部接口调用、参数兼容和失败恢复。
- `.env` / 密钥管理：涉及外部服务凭据、数据库连接、登录与 Provider 配置，禁止泄露或无授权修改。
- Prisma / SQLite 数据层：涉及 `prisma/schema.prisma`、迁移、`src/lib/prisma.ts`、`src/lib/db.ts` 与数据读写，错误改动可能造成数据损坏、迁移失败或本地状态不可回滚。

## 审查重点

- 是否越界：实际 diff 是否只包含 Hermes 允许的文件/目录。
- 是否影响点数：是否改变价格计算、点数扣减、点数调整、流水记录或余额查询。
- 是否影响登录：是否改变登录、登出、会话解析、cookie、密码校验或当前用户接口。
- 是否影响权限：是否改变管理员接口、用户启停用、middleware、API 鉴权辅助逻辑或资产归属校验。
- 是否影响数据库：是否修改 Prisma schema、迁移、数据库写命令、数据模型或查询语义。
- 是否有回滚风险：是否涉及依赖、锁文件、数据迁移、外部服务状态、上传文件或远端发布。
- 是否有测试缺口：是否缺少 lint、build、类型检查、业务流程验证或手工回归说明。
- 是否泄露敏感信息：diff 和日志中是否出现 `.env`、token、secret、cookie、数据库凭据或外部服务密钥。
