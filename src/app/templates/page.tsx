import PagePlaceholder from '@/components/PagePlaceholder';

export default function TemplatesPage() {
  return (
    <PagePlaceholder
      title="模板中心"
      description="该页面将用于整理可复用提示词模板、任务配置模板与团队共享模版。"
      currentPath="/templates"
    />
  );
}
