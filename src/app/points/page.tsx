import PagePlaceholder from '@/components/PagePlaceholder';

export default function PointsPage() {
  return (
    <PagePlaceholder
      title="积分流水"
      description="该页面将用于查看积分余额、扣减记录、发放记录与结算历史。"
      currentPath="/points"
    />
  );
}
