# 长任务状态卡住修复

## 1. 大白话目标复述

用户看到 08/07 15:18 的“篮球火”视频任务提交半小时后仍显示“生成中”。本轮要查清这条任务在后台的真实状态，并修复页面长任务状态不刷新的问题：真实失败要显示失败和退点结果，真实成功要显示已完成，不能让用户一直误以为还在生成。

## 2. 具体可执行任务

- [x] 只读查询 08/07 15:18 附近“篮球火”任务的 `VideoTask`、点数流水和成本流水，确认真实状态。
- [x] 使用现有 `finalize-pending-videos` 收尾脚本刷新仍在 `running/submitted` 的候选任务，记录处理数量和失败原因。
- [x] 修复 `src/components/generate/GeneratePageClient.tsx`：去掉最近任务轮询 10 分钟硬停止，并让页面重新加载后也能继续轮询未完成任务。
- [x] 同步检查模板生成页 `src/components/templates/TemplateGenerateClient.tsx`，避免同类入口刷新页面后丢失未完成任务轮询。
- [x] 补轻量 smoke，证明未完成任务会从最近任务列表重新进入轮询队列，且不再存在 `MAX_POLLS` 这类固定 10 分钟停止。
- [x] 跑类型检查、相关 smoke、lint/build；需要上线则部署到 `sd2.youdoodesign.com` 并验证公网新构建。
- [x] 形成聚焦 commit、push，登记版本记录。

## 3. 验收/审查内容

这些审查项需要创建独立子 agent 做只读审查；审查 agent 不改文件、不提交、不补实现，只判断是否达标、证据是否充分、风险是否遗漏，并输出“通过 / 不通过、证据、缺口、风险、下一步”。

- [x] R1. 当前任务真实状态验收
  - 检查对象：`VideoTask`、`CreditLedger`、`CostLedger` 中 08/07 15:18、15:35、15:40 的篮球任务。
  - 通过标准：能明确说清哪些任务失败、哪些任务成功、是否返还/扣除点数；不把页面旧状态当成后台真实状态。
  - 证据来源：只读 SQLite 查询和 `finalize-pending-videos` 输出。
- [x] R2. 页面刷新逻辑验收
  - 检查对象：`GeneratePageClient.tsx`、`TemplateGenerateClient.tsx` 和新增 smoke。
  - 通过标准：最近任务列表加载到 `submitted/running` 后会自动加入轮询；轮询不再 10 分钟后直接停止；终态后刷新最近任务和点数。
  - 证据来源：源码、smoke、类型检查、lint/build。
- [x] R3. 线上验收
  - 检查对象：`sd2.youdoodesign.com` 生产构建、状态接口和相关静态资源。
  - 通过标准：公网已加载包含本次轮询修复的新构建；健康周期后服务稳定。
  - 证据来源：`youdoo-sites`、公网 API、BUILD_ID 或公网静态 chunk。
- [x] R4. 独立只读审查
  - 检查对象：本轮代码 diff、验证命令、线上证据和 Git 状态。
  - 通过标准：独立子 agent 只读判断“半小时生成中”是否已修到真实页面层，不改文件、不提交、不补实现。
  - 审查结论：通过；缺口是未做已登录浏览器点击验收，另有成功任务本地缓存/缩略图超时风险需单独处理。

## 4. 审查内容是否对齐目标

- [x] A1. R1-R4 是否对齐目标
  - 判断：审查项必须证明“后台真实状态已查清 + 页面不再假卡生成中 + 线上已加载”，不能只证明脚本跑过或代码编译通过。
